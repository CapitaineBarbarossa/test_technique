<nav class="navbar navbar-expand-lg navbar-light bg-light">
	<div class="container-fluid">
		<a class="navbar-brand" href="{{ path('app_main') }}">Test Technique</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarSupportedContent">
			<ul class="navbar-nav me-auto mb-2 mb-lg-0">
				<li class="nav-item">
					<a class="nav-link active" aria-current="page" href="{{ path('app_main') }}">Accueil</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path('app_products_index') }}">Produits</a>
				</li>
			</ul>
			<ul class="navbar-nav ms-auto mb-2 mb-lg-0">
				<a class="nav-link" href="{{ path('admin')}}">Administration</a>
			</ul>
		</div>
	</div>
</nav>


<style>

	nav {
		position: relative;
		width: 100%;
		display: flex !important;
		flex-direction: row;
		align-items: center;
		background: linear-gradient(0deg, rgba(73,138,196,0.8) 0%, rgba(49,129,200,0.8) 50%, rgba(0,0,0,0.8995973389355743) 100%);
		box-shadow: inset 0 -1px 0 #c5c5c6;
		z-index: 1;
	}
</style>
